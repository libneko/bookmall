<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neko.mapper.BookStockMapper">
    <insert id="insert">
        insert into book_stock (book_id, stock)
        values (#{bookId}, #{stock});
    </insert>

    <delete id="deleteByBookIds">
        delete from book_stock where book_id in
        <foreach collection="bookIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>