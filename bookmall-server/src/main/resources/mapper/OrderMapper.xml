<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neko.mapper.OrderMapper">

    <insert id="insert" parameterType="com.neko.entity.Order" useGeneratedKeys="true" keyProperty="id">
        insert into orders (number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status,
                            amount, user_name, phone, consignee, estimated_delivery_time, delivery_time, shipping_fee)
        values (#{number}, #{status}, #{userId}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod},
                #{payStatus}, #{amount}, #{userName}, #{phone}, #{consignee}, #{estimatedDeliveryTime},
                #{deliveryTime}, #{shippingFee})
    </insert>

    <update id="update" parameterType="com.neko.entity.Order">
        update orders
        <set>
            <if test="cancelTime != null">
                cancel_time=#{cancelTime},
            </if>
            <if test="payStatus != null">
                pay_status=#{payStatus},
            </if>
            <if test="payMethod != null">
                pay_method=#{payMethod},
            </if>
            <if test="checkoutTime != null">
                checkout_time=#{checkoutTime},
            </if>
            <if test="status != null">
                status=#{status},
            </if>
            <if test="deliveryTime != null">
                delivery_time=#{deliveryTime},
            </if>
            <if test="shippingFee != null">
                shipping_fee=#{shippingFee},
            </if>
        </set>
        where id=#{id}
    </update>

    <select id="pageQuery" resultType="com.neko.entity.Order">
        select * from orders
        <where>
            <if test="number != null and number!=''">
                and number like concat('%',#{number},'%')
            </if>
            <if test="phone != null and phone!=''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="userId != null and userId!=''">
                and user_id = #{userId}
            </if>
            <if test="status != null and status!=''">
                and status = #{status}
            </if>
            <if test="beginTime != null and beginTime!=''">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null and endTime!=''">
                and order_time &lt;= #{endTime}
            </if>
        </where>
        order by order_time desc
        limit #{pageSize} offset #{offset}
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(id) from orders
        <where>
            <if test="number != null and number!=''">
                and number like concat('%',#{number},'%')
            </if>
            <if test="phone != null and phone!=''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="userId != null and userId!=''">
                and user_id = #{userId}
            </if>
            <if test="status != null and status!=''">
                and status = #{status}
            </if>
            <if test="beginTime != null and beginTime!=''">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null and endTime!=''">
                and order_time &lt;= #{endTime}
            </if>
        </where>
    </select>

    <select id="sumByMap" resultType="java.lang.Double">
        select coalesce(sum(amount), 0) from orders
        <where>
            <if test="begin != null">
                and order_time &gt; #{begin}
            </if>
            <if test="end != null">
                and order_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="begin != null">
                and order_time &gt; #{begin}
            </if>
            <if test="end != null">
                and order_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

</mapper>
